# Ember web UI overlay

These files are meant to be added to the root of `skulltombstone-hub/Ember`.

Files added:
- `index.html`
- `styles.css`
- `app.js`

Existing files are left untouched, including the no-extension file in
`assets/engines/butterscotch/`.

